# Falcon configuration — 5-parameter model (Ω_m, Ω_b, h, n_s, σ_8)
#
# All 5 cosmological parameters are sampled from uniform priors (Quijote ranges).
# No fixed_cosmo_params — everything is varied.
#
# Run with:
#   falcon sample prior --config-name config_files/config_5param.yml --run-dir outputs/5param_run

paths:
  import: "."
  graph:  "${run_dir}/graph"

logging: {}

sample:
  prior:
    n: 100

# ─────────────────────────────────────────────────────────────────────────────
graph:

  # ── Node 1: Cosmological parameters ────────────────────────────────────────
  cosmo_params:
    simulator:
      _target_: falcon.priors.Hypercube
      priors:
        - ['uniform', 0.10, 0.50]   # Ω_m
        - ['uniform', 0.03, 0.07]   # Ω_b
        - ['uniform', 0.50, 0.90]   # h
        - ['uniform', 0.80, 1.20]   # n_s
        - ['uniform', 0.60, 1.00]   # σ_8
    estimator:
      _target_: falcon.estimators.Flow
      loop:
        num_epochs: 300
        batch_size: 8
        early_stop_patience: 32
      network:
        net_type: nsf
        theta_norm: true
        norm_momentum: 0.01
      embedding:
        _target_: falcon.embeddings.LazyOnlineNorm
        _input_: [delta_fin]
      optimizer:
        lr: 0.001
        lr_decay_factor: 0.5
        scheduler_patience: 16
      inference:
        gamma: 0.5
        discard_samples: true
        log_ratio_threshold: -20
    ray:
      num_gpus: 0

  # ── Node 2: Linear matter power spectrum P(k) ──────────────────────────────
  pk:
    parents: [cosmo_params]
    simulator:
      _target_: den_pie.forward.PowerSpectrum
      dim:     3
      res:     128
      boxsize: 1000.0
    ray:
      num_gpus: 0

  # ── Node 3: Gaussian-random initial conditions ──────────────────────────────
  delta_ic:
    parents: [pk]
    simulator:
      _target_: den_pie.forward.InitialConditions
      dim:     3
      res:     128
      boxsize: 1000.0
      run_dir: ${run_dir}
    ray:
      num_gpus: 0

  # ── Node 4: N-body forward simulation (PM, bullfrog stepper, z=3 → z=0) ───
  delta_fin:
    parents: [cosmo_params, delta_ic]
    simulator:
      _target_: den_pie.forward.ForwardModel
      dim:            3
      res:            128
      boxsize:        1000.0
      sub_batch_size: 1
      sim_params:
        a_ini:                0.25
        a_end:                1.00
        stepper:              bullfrog
        method:               pm
        res_pm:               256
        time_var:             D
        alpha:                1.5
        theta:                0.5
        antialias:            0
        grad_kernel_order:    4
        laplace_kernel_order: 0
        n_resample:           1
        n_steps:              1
        deconvolve:           false
        nlpt_order_ics:       2
        worder:               2
    ray:
      num_gpus: 1.0
